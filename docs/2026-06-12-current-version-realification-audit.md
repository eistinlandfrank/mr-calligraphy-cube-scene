# 2026-06-12 当前版本功能不足与真实化开发文档

审计日期：2026-06-12  
审计入口：`http://localhost:41496/`、`http://localhost:41496/main-admin.html`、`http://localhost:41496/realistic-demo.html`、`http://localhost:41496/realistic-admin.html`  
审计结论：当前版本已经有大量本机真实能力，但还不是生产级产品。最需要改的不是继续增加 Demo，而是把“本机原型能力”升级成“可验证业务闭环”。

## 1. 当前后台入口

| 页面 | 地址 | 作用 | 当前边界 |
| --- | --- | --- | --- |
| 主场景后台 | `/main-admin.html` | 编辑主场景对象、图层、灯光、基础物体、导入模型、项目档案、本机发布和远端发布包 | 真实写入本机 `localStorage` / `IndexedDB`，但不是账号化 CMS |
| 写实样张后台 | `/realistic-admin.html` | 编辑写实桌面样张对象、模型导入、快照、发布到写实演示和远端发布包 | 真实写入本机写实场景状态，但不是服务端内容后台 |
| 前台学习页 | `/` | 10 步学习、书写练习、作品、报告、计划、档案和本机同步 | 本机浏览器真实可用，跨设备和教师端仍缺生产服务 |
| 写实演示页 | `/realistic-demo.html` | 查看写实样张发布版本 | 读取本机发布状态，不是线上发布页 |

## 2. 这一版最不完善的地方

### 2.1 本机真实和生产真实混在一起

当前很多按钮确实能用，但它们主要写入本机浏览器。例如学习状态、作品、计划、档案、报告批注、后台布局、发布版本和远端配置，大多保存在 `localStorage` / `IndexedDB`。

用户看到“发布”“同步”“教师批注”“AI 讲解”“报告”“分享”这些词，会自然理解成云端、账号、权限和跨设备能力。当前版本还没有这些生产闭环，因此 UI 必须持续强调“本机”“导出”“远端 API adapter”，不能让用户误会已经上线。

### 2.2 前台学习路径已有本机服务 adapter，但还不是云端课程引擎

前台学习路径已新增本机 `LearningPathService` adapter：`MRAppState.getLearningPathStatus()` 会用当前 `LearningTask`、`PracticeSession`、`ArtworkRecord`、`ReportRecord` 和 `PlanRecord` 推导 10 步标题、说明、焦点、完成状态、证据和下一步动作；路径面板显示真实完成数，前 6 个场景的热点正文也优先读取路径服务证据。`SCENES` 现在主要作为视觉场景和兜底配置。

它仍不是云端课程编排、教师下发任务或跨设备学习进度。后续需要继续把课程任务版本、服务端课程包、教师端排课、班级进度和跨设备同步接到同一服务接口。

### 2.3 AI 讲解已有本机服务 adapter，但不是云端 AI

当前讲解能力已从直接调用浏览器 `speechSynthesis` 升级为本机 `LectureService` adapter：前台会检查浏览器本机语音能力，记录语音名称、播放段落、文本计时降级次数、失败次数和完成时间，并在讲解面板显示服务边界。这是可验证的本机讲解服务，但仍不是云端 AI 音频、真人录音、视频流，也不是根据学生真实笔迹生成的动态反馈。

真实化方向：

- UI 文案保留“本机语音讲解”边界。
- 已新增 `LectureService` adapter，当前支持 `local-tts` 和 `local-text-timer`；后续扩展 `remote-ai-audio`、`remote-ai-text` 来源。
- 把讲解生成结果写入报告和学习记录，避免只在界面临时播放。

### 2.4 评分已有本机服务 adapter，但不是专业书法识别模型

当前评分已经会读取真实笔迹点位，并输出结构、笔画、笔法、力度、流畅度等基础评分证据；本轮新增本机 `ScoreService` adapter，会迁移旧练习评分记录，记录评分来源、算法版本、最近分数、最近证据摘要、累计评分次数和采样点，并在前台评分卡显示“本机基础评分”边界。但它仍是浏览器端启发式算法，不是训练模型，也没有范字路径库、笔顺模型、硬件压感校准和教师评分标定。

真实化方向：

- 增加范字路径库和笔顺数据。
- 保存设备、压力、采样率和画布尺寸。
- 已增加评分版本号、评分证据说明和 `ScoreService` adapter。
- 后续在 `ScoreService` 上扩展服务端模型、教师标定和硬件适配来源。

### 2.5 报告和教师批注已有远端 adapter，但生产报告仓库还没完成

HTML 报告、原生 PDF、PDF 能力条形图、PDF 能力雷达图、PDF 分数趋势图、PDF 最近作品 JPEG 截图嵌入、报告对比、多报告趋势、本机教师批注、本机验真摘要、报告仓库本机 JSON 同步包、报告仓库远端 API adapter、报告仓库签名回执审计导出和报告冲突审计已经可用，刷新后也能复现。`MRAppState.getReportVerification()` 会用稳定 JSON 为报告核心字段、教师批注、关联练习和最近作品截图摘要计算 SHA-256，并写入 HTML/PDF 导出；`MRAppState.getReportRepositoryPackage()` 会把报告和摘要打包成 `mr-calligraphy-report-repository-v1`，前台可下载同步包或导入同格式 JSON 包，也可配置 endpoint/token 后真实 GET 检查、PUT 推送和 GET 拉取；远端返回完整 `receipt/latestReceipt` 时会保存到 `reportRepository.lastSignedReceipt` 和 `reportRepository.signedReceipts`，前台可查看最近回执并导出包含签名、仓库摘要、回执摘要、方向、endpoint 和原始 JSON 的 HTML 审计页；同 ID 差异报告会写入 `reportRepository.lastConflictReports`，前台可字段级合并、另存远端副本或忽略审计。

但它仍不是生产报告产品。教师批注默认仍来自当前浏览器里的报告字段，本机验真摘要不是账号化教师签名、服务端证书或不可篡改审计；当前签名回执审计是 mock/HMAC 级开发验收和前端证据留存，不是生产证书链、教师身份签章或服务端不可篡改日志；报告仓库远端 API 也只是可替换后端的第一版 adapter，不是账号空间、教师权限、长期归档或服务端 PDF 渲染。

真实化方向：

- 固定 `ReportRecord` schema 和版本迁移。
- 把当前 `ReportRepository` 远端 adapter 升级为账号化服务端仓库。
- 把当前本机报告冲突审计和 mock 签名回执升级为服务端版本合并、教师身份审计、生产证书签名和验签。
- 教师批注增加 reviewerId、role、签名、审计记录和服务端时间。
- PDF 已补原生能力雷达图和分数趋势图，后续继续增加生产服务端验真证书和服务端渲染。

### 2.6 分享能力只是导出文件

当前作品分享页和报告分享已包含离线 HTML / PDF / WebM 文件导出，并新增本机分享链接服务：作品复盘区可以生成 `?share=...` 本机链接、复制链接、记录访问/复制次数、撤销链接，并在同一浏览器内通过分享路由打开作品详情。本轮又新增远端分享 API adapter：用户可配置 endpoint/token，前台真实 GET 检查服务、PUT 发布 `mr-calligraphy-share-repository-v1` 分享包，保存远端 `publicUrl` 和回执。这些都属于真实本机/adapter 能力，但仍不是内置公网托管、课堂发布、社群分享、微信分享或生产权限服务。

真实化方向：

- 保持离线分享页按钮为 `real-export`。
- 已新增本机 `ShareService`，支持生成本机链接、撤回链接、过期时间、复制/访问计数和 `ShareRecord` 留痕。
- 已新增远端分享 API adapter，支持用户自备 endpoint、Bearer token、publicUrl 和回执留痕。
- 后续若接入生产后端，再把 `ShareService` 扩展为内置公开链接、访问权限、班级作品墙和跨设备发布。

### 2.7 后台还不是可协作的项目后台

主后台和写实后台都能编辑本机场景，也能导出项目档案、做本机发布、回滚、远端发布包预检、服务端锁预检和回执审计。本轮新增本机 `ProjectRepository` 状态视图：`projectSchema.repository` 会把主场景和写实样张归一为 `project-scene-repository-v1`，统一统计草稿对象、快照、发布版本、导入资产、缺文件/缺哈希风险和下一步动作；主后台项目档案区会直接显示这份仓库状态。随后又补了远端项目仓库 API adapter，主后台可配置 endpoint/token，用真实 GET 检查服务、PUT 当前项目仓库包、读取远端版本历史，并用 `GET ?packageId=` 拉取指定历史包进入恢复预览，同时保存服务端回执。

它们仍不是账号后台，任何能打开页面的人都能改当前浏览器里的数据，`ProjectRepository` 远端 adapter 只是可替换服务端接口的第一版，不是多人协作 CMS。

真实化方向：

- 继续扩展主后台和写实后台对象 schema 的字段迁移和完整 diff；统一仓库状态第一版已完成。
- 把远端 `ProjectRepository` adapter 继续生产化为账号化服务端 repository、多人合并和资产签名。
- 接入账号、角色、权限、审计和发布审批。
- 远端发布由“用户配置 endpoint”升级为“生产发布服务”。

### 2.8 远端 API adapter 有了，但服务端产品没有

计划仓库、学习档案仓库、报告仓库、项目仓库和后台远端发布都已有 API 合同或 mock server、endpoint/token 配置、GET/PUT/POST 检查和本机状态持久化；计划仓库、报告仓库和项目仓库都能保存远端回执并导出本机 HTML 审计页；学习计划也能导出标准 `.ics` 日历提醒文件，便于导入系统日历或手机日历。这个方向是对的，但它目前证明的是“前端能对接 API / 本机能导出标准文件 / 本机能留存回执证据”，不是“项目已有生产后端”。

真实化方向：

- 补生产服务端仓库。
- 补登录态和 token 刷新。
- 将当前 `.ics` 本机日历导出升级为账号化提醒、教师端通知和后台任务下发。
- 补账号化分页查询、服务端冲突合并、生产签名回执和不可篡改审计；前端 adapter 已能按 `nextPageUrl` 自动追取学习档案分页，并把同 ID 差异保存为本机冲突审计和字段级合并表单，计划仓库也已能保存远端 receipt 到本机审计列表。
- mock server 保留为本地开发验收，不再作为产品能力宣传。

### 2.9 浏览器级验收还不够

现在 smoke test 很强，能检查语法、页面、状态层、mock server 和很多数据函数。Playwright 也已有第一批用例，并覆盖站内报告 PDF 下载、能力雷达图标记、分数趋势图标记与作品截图嵌入；但测试覆盖还不足以证明所有复杂 UI 都真实可用，尤其是导入模型、教师批注更多边界、远端接口失败、移动端布局和 WebGL 非空渲染。

真实化方向：

- 扩展 Playwright 覆盖报告教师批注、学习档案同步、计划冲突处理、项目档案恢复和远端发布回执。
- 给 WebGL canvas 加非空像素检查。
- 下载类功能校验文件名、MIME 和关键内容。
- 移动端视口检查后台面板不重叠。

## 3. 当前控件清单

本轮运行：

```bash
node scripts/control-inventory.js --check
```

结果：

| 来源 | `real-local` | `real-export` | `real-published-local` | `demo-content` | `disabled` | 缺失/非法 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `index.html` | 72 | 21 | 0 | 0 | 0 | 0 |
| `main-admin.html` | 38 | 6 | 1 | 0 | 0 | 0 |
| `realistic-demo.html` | 3 | 0 | 0 | 0 | 0 | 0 |
| `realistic-admin.html` | 24 | 2 | 1 | 0 | 0 | 0 |
| `script.js dynamic` | 29 | 1 | 0 | 0 | 1 | 0 |

判断：控件标记层面已经过关，没有明显未标记按钮。下一步不是继续数按钮，而是逐个验证 `real-local` 的真实边界是否在 UI、文档、状态和测试里讲清楚。

## 4. “假界面”真实化标准

一个前端入口只有同时满足以下条件，才能继续标记为真实能力：

| 条件 | 要求 |
| --- | --- |
| 数据来源 | 明确读取 state、storage key、IndexedDB、文件或 API |
| 执行动作 | 明确调用函数、写入字段、生成文件或发起请求 |
| 成功反馈 | 页面状态、文件下载、版本号、记录数或回执发生可见变化 |
| 失败反馈 | 未配置、无数据、权限不足、浏览器不支持、网络失败时有明确提示 |
| 持久化 | 刷新、切页、重新打开或重新导入后能复现 |
| 自动验收 | 至少有 smoke、数据层脚本或 Playwright 覆盖核心路径 |
| 边界说明 | 本机、导出、远端 adapter、生产云端必须分清楚 |

不能再出现的情况：

- 点击后只显示“成功”，但没有数据写入或文件产物。
- 文案写“发布/同步/分享”，实际只是本机状态，却没有边界说明。
- mock server 能跑，就把功能说成已经有生产服务。
- 按钮可点击，但失败时只有控制台错误。

## 5. 建议真实化路线

### P0：把高预期功能的边界写清楚

交付：

- 所有“AI、评分、同步、发布、分享、教师批注、后台”文案增加本机/导出/远端 adapter 边界。
- 远端未配置时，检查/推送按钮保留明确禁用或状态提示。
- README 和页面状态区都说明：当前静态项目无账号后端。

验收：

```bash
node scripts/control-inventory.js --check
node scripts/smoke-test.js --base-url=http://localhost:41496/
```

### P1：补浏览器级真实交互测试

交付：

- Playwright 覆盖报告教师批注保存、刷新复现、清除批注。第一版已补入 `tests/e2e/real-flows.spec.js`。
- 覆盖学习档案远端同步成功和失败。成功路径第一版已补入前台 E2E，包含检查、推送、拉取和状态持久化；失败路径已覆盖 401 token 过期、500 服务端故障、非法 JSON、远端可访问但无档案包、422 推送拒收、网络中断、分页返回提示、`nextPageUrl` 自动追取、同 ID 差异跳过、冲突审计、字段级合并和远端冲突另存副本。
- 覆盖计划冲突解决三种策略。数据层和浏览器级用例均已覆盖另存副本、保留本机、采用远端三种策略。
- 覆盖计划仓库远端失败反馈。浏览器级用例已覆盖 401 token 过期、500 服务端故障、非法 JSON、远端可访问但无计划包、PUT 422 拒收和 PUT 网络中断。
- 覆盖主后台远端发布回执导出。第一版已补入主后台 E2E，包含模拟远端 API、审核、推送、回执持久化和导出。
- 覆盖 WebGL canvas 非空像素。第一版已补入前台主房间、主后台和写实后台 E2E。

验收：

```bash
npm run test:e2e
```

### P1：把课程路径改成任务驱动

交付：

- 第一版已完成：新增本机 `LearningPathService`，`SCENES` 主要保留视觉场景和兜底配置。
- 学习任务、步骤完成条件、下一步动作已由 `LearningTask` 和状态层推导。
- 前 6 个场景的静态热点说明已接入真实任务、练习、作品、报告和计划状态；后续继续扩展课程包和教师端编排。

验收：

- 清空本机状态后，不显示伪造完成度。
- 完成一次练习、保存作品、生成报告后，步骤完成状态真实变化。

### P2：补账号化 repository

交付：

- `PlanRepository`、`HistoryRepository`、`ReportRepository`、`ProjectRepository` 服务端草案和最小实现；`ReportRepository` 与 `ProjectRepository` 远端 API adapter 第一版已完成，仍需账号空间和服务端合并。
- 登录态、用户 ID、角色、token 刷新和服务端分页。
- 计划仓库冲突解决已从计划级扩展到前端字段级第一版，远端 receipt 也已能写入本机审计列表并导出 HTML；后续仍需服务端合并审计、账号空间隔离和更复杂的计划项增删合并。

验收：

- 两个浏览器或两台设备能读取同一账号数据。
- 断网、token 过期、远端冲突时都有可恢复状态。

### P2：升级评分和讲解服务

交付：

- `ScoreService` adapter 第一版已落地，当前记录本机启发式评分来源、算法版本和证据摘要。
- `LectureService` adapter 第一版已落地，当前记录本机语音能力、降级和完成状态。
- 范字路径库、笔顺库、压感字段和评分版本。
- 本机 fallback 与服务端结果可并存。

验收：

- 同一笔迹可重复得到同一评分版本结果。
- 服务端不可用时仍保留本机基础评分。
- 报告能显示评分来源、版本和证据。

### P3：后台项目化和生产发布

交付：

- 统一主后台/写实后台对象 schema；本机 `ProjectRepository` 状态视图和远端项目仓库 API adapter 第一版已完成。
- 项目仓库版本历史拉取第一版已完成；后续继续支持账号化远端保存、服务端字段合并、资产签名。
- 远端发布服务支持审批、发布锁、CDN 回执和不可篡改审计。

验收：

- 项目档案可跨浏览器恢复。
- 服务端能拒绝摘要不一致、权限不足和重复发布。
- 发布回执可以在本机和服务端双向核对。

## 6. 下一个开发批次建议

我建议下一批不要先做新页面，而是按下面顺序推进：

1. 扩展 Playwright：先把报告教师批注、远端回执、计划冲突和学习档案同步跑成浏览器级用例。
2. 清理高预期文案：把“本机真实”和“生产真实”的边界放到页面状态区。
3. 任务驱动化：把 `SCENES` 里的学习说明逐步迁移到 `LearningTask` 和状态层。
4. 账号化 repository 设计：先写服务端合同和最小 mock，再接真实服务。
5. 后台 schema 统一：减少主后台和写实后台长期分叉。

## 7. 本轮开发记录

完成内容：

- 新增本审计文档，明确当前版本哪些能力仍不完善、哪些 UI 容易被误认为生产功能，以及后续真实化路线。
- 重新运行控件状态清单，确认当前 HTML 和动态控件没有缺失状态标记。
- 把“按钮能点”提升为“有数据来源、写入、反馈、持久化和自动验收”的真实化标准。
- 追加报告教师批注浏览器验收记录：E2E 已覆盖保存批注、写入 `ReportRecord.teacherReview`、刷新复现和清除批注。
- 追加 WebGL 画布非空验收记录：E2E 会采样前台、主后台和写实后台 canvas 像素，避免只检查 DOM 存在。
- 追加主后台远端发布回执验收记录：E2E 已覆盖 endpoint/token、检查远端、审核、推送、回执持久化和回执审计 HTML 下载。
- 追加学习档案远端同步验收记录：E2E 已覆盖 endpoint/token、检查远端、PUT 推送同步包、GET 拉取同步包和 `historyRepository` 状态持久化。
- 追加计划仓库冲突验收记录：E2E 已覆盖生成真实计划、配置远端 API、推送计划包、本机/远端同时修改、拉取触发冲突面板和“另存副本”解决路径。
- 追加计划仓库三策略数据层验收：`learning-state-check.js` 已覆盖冲突另存副本、保留本机推送、采用远端强制拉取和最终持久化状态。
- 追加计划仓库三策略浏览器验收：E2E 已覆盖“另存副本”“保留本机”“采用远端”三个按钮路径，并读取本机状态和远端请求体确认真实生效。
- 追加计划仓库字段级合并验收：E2E 已覆盖在冲突面板选择远端计划标题和远端任务说明、保留本机任务标题，并确认混合结果写入本机计划且进入待同步队列。
- 追加计划仓库推送失败浏览器验收：E2E 已覆盖 PUT 422 拒收和 PUT 网络中断，并确认失败后 `pendingAutoSync` 继续保留、本机计划不被清空。
- 追加学习档案分页冲突浏览器验收：E2E 已覆盖 GET 网络中断、分页响应提示、拉取时自动请求第二页、同 ID 差异跳过和字段级合并，确认远端多页新增记录写入本机且冲突记录只按用户选择写回字段。
- 追加全量浏览器验收记录：`npm run test:e2e` 当前 11 条用例全部通过，覆盖前台真实书写/报告/学习档案同步、学习档案失败反馈、学习档案分页冲突、学习档案字段级合并、计划冲突三策略、计划字段级合并、计划仓库失败反馈、主后台远端发布回执和写实后台发布回滚。
- 追加计划仓库远端失败浏览器验收：E2E 已覆盖 401、500、非法 JSON、无计划包、PUT 422 拒收和 PUT 网络中断，并确认错误写入 `planRepository.lastError`、空仓库状态写入 `lastRemoteStatus`，推送失败保留待同步队列。
- 追加学习档案远端失败浏览器验收：E2E 已覆盖 401、500、非法 JSON、无档案包、422 推送拒收、网络中断、分页返回、`nextPageUrl` 自动追取和同 ID 差异跳过，并确认错误写入 `historyRepository.lastError`、空仓库或分页状态写入 `lastRemoteStatus`。
- 追加学习档案分页自动追取记录：`pullHistoryRepositoryFromRemote()` 现在会最多追取 20 页、检测分页循环，并把实际处理记录数、页数、新增数量和冲突跳过数量写回本机状态。
- 追加学习档案冲突审计记录：同 ID 差异会写入 `historyRepository.lastConflictRecords`，前台可审阅字段差异，并可将远端冲突记录另存为本机副本。
- 追加学习档案字段级合并记录：前台冲突审计区可逐字段选择本机或远端，数据层和 E2E 已验证只写回用户选择的远端字段。
- 追加远端项目仓库版本历史记录：主后台远端项目仓库 API 已支持 `versions`、版本选择和 `GET ?packageId=` 指定版本拉取，E2E 已验证两次推送后选择旧版本进入恢复预览。
- 追加报告本机验真摘要记录：前台报告详情、HTML 导出和原生 PDF 都会展示同一份 SHA-256 摘要，`learning-state-check.js` 已验证摘要可复算且会随教师批注变化。
- 追加报告仓库远端同步记录：站内报告面板已支持远端报告 API endpoint/token、检查、推送、拉取；数据层、mock server 和 E2E 已验证报告包、教师批注、本机验真摘要、Bearer token、远端回执和同 ID 差异跳过。
- 追加报告仓库冲突审计记录：同 ID 差异报告会写入 `reportRepository.lastConflictReports`，前台可审阅字段差异，按字段采用远端值，也可另存远端报告副本；数据层和 E2E 已验证真实写回。
- 追加报告仓库签名回执审计记录：远端 `receipt/latestReceipt` 会写入 `lastSignedReceipt` 和最近 12 条 `signedReceipts`，站内报告可查看回执摘要并导出 HTML 审计页；数据层和 E2E 已验证签名、摘要、方向和导出文件内容。
- 追加学习计划日历提醒导出记录：`MRAppState.getPlanCalendarExport()` 会从真实计划项生成 `.ics`，前台“导出日历”可下载包含 `VCALENDAR`、`VEVENT` 和 `VALARM` 的文件；数据层和 E2E 已验证结构与下载内容。
- 追加计划仓库回执审计记录：远端计划 API 返回的 `receipt/latestReceipt` 会写入 `lastReceipt` 和最近 12 条 `receipts`，前台计划远端同步区可查看回执摘要并导出 HTML 审计页；数据层和 E2E 已验证摘要、方向、endpoint 和导出文件内容。
- 追加项目仓库回执审计记录：远端项目仓库 API 返回的 `receipt/latestReceipt` 会写入 `mr-calligraphy-project-repository-remote-v1.receipts`，主后台远端项目仓库面板可查看回执摘要并导出 HTML 审计页；E2E 已验证 packageId、sourcePackageId、receiptDigest、方向和下载文件内容。
- 追加项目仓库远端失败反馈记录：主后台项目仓库远端 adapter 会拒绝非 JSON 响应，HTTP 错误、无项目包和网络中断会写入 `lastError`；E2E 已验证 401、非 JSON、无项目包、PUT 422、网络中断和失败后本机布局保留。
- 追加报告教师批注审计记录：保存和清除教师批注会写入 `mr-calligraphy-learning-state-v1.reportTeacherReviewAudits`，前台报告详情可查看最近审计并导出 HTML；数据层和 E2E 已验证 `save/clear`、SHA-256 摘要、预览、下载文件和刷新持久化。
- 追加项目档案恢复审计摘要记录：恢复成功后会写入 `archiveDigest`、`selectionDigest` 和 `recordDigest`，主后台恢复审计列表显示摘要短码，HTML 导出包含完整摘要和原始 JSON；数据层和 E2E 已验证真实恢复、刷新持久化和下载文件。
- 追加书写视频封面和导出记录：前台复盘页导出 WebM 后会生成 PNG 封面，写入 `mr-calligraphy-learning-state-v1.videoExportService.records`，复盘面板显示导出摘要并可下载封面；数据层和 E2E 已验证 WebM 下载、封面记录、PNG 下载和刷新后持久化。
- 追加书写视频导出队列和失败重试：每次导出会写入 `videoExportService.jobs`，状态覆盖排队、生成中、已完成和失败；失败任务在复盘页显示错误和重试按钮，E2E 已验证禁用 `MediaRecorder` 后失败、恢复录制能力后重试下载 WebM。
- 追加作品分享远端 API adapter：复盘区可配置远端分享 endpoint/token，真实 GET 检查、PUT 发布分享包，并保存 publicUrl、packageId 和回执；新增分享 API 合同、本机 mock server、数据层断言和 E2E 前台按钮验收。
- 追加作品分享远端回执审计记录：远端分享 API 返回的 `receipt/latestReceipt` 会写入 `shareService.receipts`，复盘区可查看最近回执并导出 HTML 审计页；数据层和 E2E 已验证无回执不可导出、publicUrl、receiptDigest 和下载文件内容。
- 追加作品分享远端撤销记录：复盘区新增“撤销远端”，会对分享 endpoint 发起真实 DELETE，请求 `mr-calligraphy-share-repository-revoke-v1`，并把 `remoteRevokedAt`、撤销回执和审计导出写回本机状态；数据层和 E2E 已验证撤销请求体、Bearer token、撤销回执和下载文件内容。
- 追加主后台基础物体更新记录：“更新所选”不再被 HTML 静态禁用，改由选中对象状态动态控制；E2E 已验证新增基础物体后修改名称、类型、颜色、半径和高度，并确认草稿、发布版本和前台发布布局都读取更新后的规格。
- 追加写实后台删除恢复记录：E2E 已覆盖删除当前写实对象、恢复对象、再次删除后撤回，并读取 `mr-calligraphy-realistic-layout-v1` 确认 `deleted` 字段随 UI 操作持久化。
- 追加主后台导入模型删除审计记录：导入模型删除后会写入 `mr-calligraphy-main-import-audit-v1`，记录模型 ID、文件名、SHA-256、文件大小、历史快照引用和清理结果；E2E 已验证真实 `.glb` 导入、删除、刷新持久化和 HTML 审计导出。
- 追加写实后台导入模型软删除审计记录：写实导入模型删除/恢复后会写入 `mr-calligraphy-realistic-import-audit-v1`，记录模型 ID、文件名、SHA-256、文件大小和软删除结果；E2E 已验证真实 `.glb` 导入、软删除、恢复、刷新持久化和 HTML 审计导出。
- 追加主后台导入模型外观编辑记录：导入模型现在可编辑主色调，写入 `mr-calligraphy-main-scene-layout-v1.importedModels[*].color`，发布后进入 `mr-calligraphy-main-scene-published-v1`；E2E 已验证真实 `.glb` 导入、更新颜色、草稿持久化、发布持久化和前台发布布局读取。
- 追加写实后台导入模型外观编辑记录：写实导入模型现在可编辑主色调，写入 `mr-calligraphy-realistic-layout-v1.importedModels[*].color`，发布后进入 `mr-calligraphy-realistic-published-v1`；E2E 已验证真实 `.glb` 导入、更新颜色、草稿持久化、发布持久化和写实演示页布局读取。
- 追加导入模型透明度编辑记录：主后台和写实后台导入模型现在可编辑透明度，写入各自 `importedModels[*].opacity`，发布后进入对应 published layout；普通前台 WebGL 顶点已扩展为 RGBA，E2E 已验证真实 `.glb` 导入、更新透明度、草稿持久化、发布持久化和演示页布局读取。
- 追加导入模型文件替换记录：主后台和写实后台导入模型现在可替换 GLB/OBJ 文件，保留原对象 ID 和布局变换，更新 `fileName/type/sha256/metrics` 并覆盖 IndexedDB 二进制；E2E 已验证真实 `.glb` 导入、替换为另一个 `.glb`、草稿持久化、发布持久化和演示页布局读取。
- 追加导入模型 PBR 参数编辑记录：主后台和写实后台导入模型现在可编辑粗糙度和金属度，写入各自 `importedModels[*].roughness/metalness`，发布后进入对应 published layout；普通前台 WebGL 已新增材质 attribute 和 shader 高光/金属感计算，E2E 已验证真实 `.glb` 导入、PBR 更新、草稿持久化、发布持久化和演示页布局读取。
- 追加 GitHub 代理重试记录：本机代理可用，`git push origin main` 已通过代理返回远端一致，GitHub 连接器也能读取最新中文提交；后续提交继续用本机代理参数推送。
- 追加导入模型发布差异明细记录：主后台和写实后台发布差异现在能显示导入模型文件、SHA、颜色、透明度、粗糙度、金属度和位置变化；E2E 已验证新增导入模型材质摘要和发布后草稿再次修改时的旧值/新值对比。
- 追加写实导入模型已删除文件物理清理记录：写实后台新增“清理已删除文件”，会对已软删除导入模型执行 IndexedDB 文件删除并从写实草稿移除记录；E2E 已验证软删除、恢复、再次删除、确认清理、IndexedDB 记录消失、刷新审计和 HTML 导出。
- 追加前台学习详情总结记录：第 10 步“查看详情”现在读取本机学习路径、练习、作品、报告和计划，展示结构化详情面板；E2E 已验证真实练习、保存作品、导出报告后能显示本机学习详情。
- 追加移动端视口验收记录：Playwright 现在以 390×844 手机视口打开前台、主后台、写实后台和写实演示页，检查横向溢出、核心面板边界、面板遮挡和 canvas 非空渲染；并修复写实后台移动端风险提示遮住编辑面板的问题。

已知限制：

- 当前新增的是用户自备 endpoint 的远端分享 adapter、本机回执审计和撤销请求、前台本机学习详情总结、核心入口移动端视口验收、主后台基础几何体更新闭环、写实后台对象删除恢复验收、主后台导入模型删除审计、写实导入模型软删除审计、写实导入模型已删除文件本机清理、主后台导入模型主色调编辑、写实后台导入模型主色调编辑、导入模型透明度编辑、导入模型文件替换、导入模型 PBR 参数编辑、导入模型发布差异明细；它们不是内置账号系统、微信分享、班级作品墙、生产 CDN、服务端权限审计、CDN purge、不可篡改日志、导入模型贴图替换、远端资产签名、云端垃圾回收、完整移动设备矩阵或多人协作审计；视频队列仍是页面打开期间的本机队列，不是 MP4/GIF 转码、服务端压缩或 Service Worker 后台队列。
- 当前服务器已经能访问 `http://localhost:41496/main-admin.html`；文档内验收命令基于该本机服务。

建议提交信息：

- `真实化移动端视口验收`
