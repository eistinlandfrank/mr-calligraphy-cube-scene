# 旧版主线开发执行清单

> 日期：2026-06-14
> 规则：完成一个功能、通过验收、更新文档后，再用中文提交并推送到 GitHub。

---

## 勾选规则

1. 完成且验收通过才勾选。
2. 只做静态展示、假按钮、假数据，不得勾选。
3. 新增任务只能追加，不改旧编号含义。
4. 当前主线是旧版静态 3D / MR 项目，不再新增 Vite / React 新应用任务。
5. 后台控制页面固定为 `main-admin.html`，不能被新页面替代。

---

## S0 旧版主线恢复

- [x] **S0-01：恢复旧版项目为主入口**
  验收：`index.html` 是前台主入口，`main-admin.html` 是后台主入口。

- [x] **S0-02：删除新项目入口**
  验收：删除 `app/`、`vite.config.mts`、`tsconfig.json`、`tests/e2e/vite-app.spec.js`。

- [x] **S0-03：恢复静态服务器脚本**
  验收：`npm run dev` 启动 `0.0.0.0:41496`，不再启动 Vite 5173。

- [x] **S0-04：修正开发文档**
  验收：README、开发计划、执行清单都明确旧版静态项目是当前主线。

完成记录（2026-06-14）：

- 已删除 Vite / React 新项目。
- 已恢复静态服务器脚本。
- 已修正文档入口和局域网访问端口。

---

## S1 旧版布局一致性

- [x] **S1-01：保留旧版前台场景布局**
  验收：前台仍使用旧版立方体房间、旧版空间布局和旧版物体坐标体系。

- [x] **S1-02：保留旧版主后台**
  验收：`main-admin.html` 可访问，主场景对象控制和本机审计仍存在。

- [x] **S1-03：保留写实样张入口**
  验收：`realistic-demo.html` 和 `realistic-admin.html` 仍可访问。

- [x] **S1-04：做一次旧版布局视觉回归截图归档**
  验收：保存前台、主后台、写实样张、写实后台截图，并记录截图路径。

完成记录（2026-06-14）：

- 前台、主后台和写实入口仍作为冒烟测试页面检查。
- 前台截图：`/tmp/mr-calligraphy-old-workflow-front.png`。
- 主后台截图：`/tmp/mr-calligraphy-old-workflow-admin.png`。

---

## S2 交互真实化

- [x] **S2-01：移除可见演示数字步骤导航**
  验收：前台底部导航显示 VR 功能舱名称，不再显示演示编号。

- [x] **S2-02：建立 VR 菜单路由**
  验收：支持 `?flow=practice`、`?flow=report` 等真实菜单路由。

- [x] **S2-03：保留旧链接兼容**
  验收：历史 `?step=` 链接仍能映射到对应功能舱。

- [x] **S2-04：菜单动作覆盖检查更新**
  验收：冒烟测试覆盖真实菜单动作，不再使用旧演示口径。

- [ ] **S2-05：逐项清理假按钮**
  验收：每个按钮都归类为本机真实、本机预览、远端 Adapter 或暂不可用，并有操作回执。

完成记录（2026-06-14）：

- 前台已从演示步骤改为 VR 主页、任务舱、讲解舱、书写台、笔画台、创作台、档案舱、复盘舱、报告舱、巩固舱。

---

## S3 AR / VR 空间表现

- [x] **S3-01：新增空间模式面板**
  验收：前台存在 MR 交互、AR 锚点、VR 环视三种模式。

- [x] **S3-02：空间模式有真实视觉差异**
  验收：切换模式会改变热点、面板、锚点或沉浸显示状态。

- [x] **S3-03：移动端空间面板回归检查**
  验收：390px 宽度下空间面板不遮挡核心操作。

- [ ] **S3-04：真实 WebXR 能力调研**
  验收：记录目标设备、浏览器支持、权限要求和降级策略。

完成记录（2026-06-14）：

- 前台已新增空间模式面板与 `body[data-spatial-mode]` 状态。
- Playwright 移动端布局测试已通过，390px 宽度下核心面板未发生横向溢出或关键遮挡。

---

## S4 后台控制真实化

- [x] **S4-01：确认后台控制页面**
  验收：后台页面为 `main-admin.html`。

- [x] **S4-02：保留本机审计与发布边界**
  验收：后台有本机操作审计、发布记录、远端 Adapter 配置与边界说明。

- [ ] **S4-03：主后台物件编辑可用性审计**
  验收：逐项验证新增、更新、删除、恢复、材质编辑、贴图清理是否真实生效。

- [ ] **S4-04：后台操作说明改为真实状态文案**
  验收：后台没有暗示已经具备真实云端生产管理能力。

---

## S5 验收与发布

- [x] **S5-01：更新冒烟检查入口**
  验收：`npm run build` 检查旧版静态入口、主后台、写实样张和写实后台。

- [x] **S5-02：运行完整 Playwright 回归**
  验收：`PLAYWRIGHT_BASE_URL=http://localhost:41496/ npx playwright test tests/e2e/real-flows.spec.js` 通过。

- [x] **S5-03：中文提交并推送 GitHub**
  验收：GitHub `main` 分支包含本轮恢复旧版主线改动。

---

## S6 服务器本地布局保存

- [x] **S6-01：新增本地部署服务器 API**
  验收：`npm run dev` 启动 Node 服务器，提供 `/api/main-scene/layout` 和 `/api/main-scene/published`。

- [x] **S6-02：后台保存写入服务器本地**
  验收：后台保存草稿、发布前台、回滚发布版本时优先写入 `server-data/main-scene-*.json`。

- [x] **S6-03：前台读取后台服务器布局**
  验收：前台优先读取服务器发布布局；尚未发布时读取服务器草稿布局；`?mainScenePreview=draft` 直接读取服务器草稿布局。

- [x] **S6-04：浏览器缓存降级**
  验收：API 不可用时仍能用 localStorage 缓存打开，不把 cookie 当作布局存储。

完成记录（2026-06-15）：

- 已新增 `scripts/local-server.js` 和 `main-scene-local-store.js`。
- `server-data/` 已加入 `.gitignore`，部署本地保存文件不会误提交。
- 已验证 Node 本地服务器 API 可写入和读取主场景布局 JSON。
- 已验证 `main admin publishes a local draft that the front page reads` E2E：后台发布后前台读取同一份布局。
- 当前 `41496` 已切换为 Node 本地服务器，`/api/main-scene/layout` 可访问。

## S7 前后台主场景渲染统一

- [x] **S7-01：前台改用后台同风格 Three.js 渲染**
  验收：前台进入 `front-three-admin-renderer`，不再使用旧前台自绘 WebGL 家具渲染作为 HTTP 模式主路径。

- [x] **S7-02：迁移后台灯光与环境**
  验收：前台使用 RoomEnvironment、半球光、主聚光灯、边缘方向光、ACES 曝光、阴影和环境强度。

- [x] **S7-03：物体位置与后台布局一致**
  验收：前台使用后台同一份模型默认坐标、旋转、缩放、大型 GLB 缩放因子和服务器本地布局状态。

- [x] **S7-04：前台保留模型加载可观测性**
  验收：前台继续写入 `MR_LOADED_MODEL_COUNT`、`MR_LOADED_MODEL_VERTICES`、`MR_LOADED_TEXTURED_MODEL_COUNT`。

完成记录（2026-06-15）：

- 已新增 `front-main-scene-renderer.js`。
- 已为 `index.html` 补齐 Three.js import map。
- 已验证前台从 `server-local` 读取主场景布局，加载 19 个 GLB 模型，并进入 `front-three-admin-renderer`。
- 已验证移动端核心面板用例等待新渲染器启动后通过。

## S8 前台跟随后台最新保存与视野遮挡控制

- [x] **S8-01：前台读取最新服务器布局**
  验收：普通前台同时检查服务器草稿和发布版；草稿更新时间更晚时读取草稿，发布版更新时间更晚时读取发布版。

- [x] **S8-02：解释并修正后台保存后前台不变的问题**
  验收：后台只保存草稿但已有旧发布版时，前台刷新会显示最新草稿，不再被旧发布版挡住。

- [x] **S8-03：主面板可收起**
  验收：AI 书法教练主面板标题条右侧可收起/展开，收起后只保留小标题条和拖动能力。

- [x] **S8-04：折叠状态本机持久化**
  验收：折叠状态写入当前浏览器本机缓存，刷新后保持。

完成记录（2026-06-15）：

- 已验证“旧发布 + 新草稿”时前台来源为 `draft-fallback/server-local` 并读取草稿测试物体。
- 已验证主面板折叠后高度约 56px，按钮语义切换为“展开主面板”。

## S9 删除后台访问门禁

- [x] **S9-01：移除主后台访问码 UI**
  验收：`main-admin.html` 不再包含访问门禁、访问码、解锁后台或锁定后台控件。

- [x] **S9-02：移除写实后台访问码 UI**
  验收：`realistic-admin.html` 不再包含访问门禁、访问码、解锁后台或锁定后台控件。

- [x] **S9-03：权限判断不再依赖门禁会话**
  验收：`admin-audit.js` 的 `canPerform()` 只根据本机操作者角色判断，不再读取 sessionStorage 门禁状态。

- [x] **S9-04：保留角色权限和操作审计**
  验收：复核角色仍只读，编辑/负责人/本机管理员仍按角色恢复对应后台能力，审计记录继续可导出。

完成记录（2026-06-15）：

- 已删除主后台和写实后台访问码面板。
- 已删除解锁/锁定后台脚本逻辑和 `access-unlock` / `access-lock` 审计动作。
- 已更新 smoke 标记和 Playwright 权限用例。

## S10 前台 VR 交互菜单

- [x] **S10-01：移除十个步骤的外显模型**
  验收：前台标题、左侧面板、底部导航、服务摘要和动作详情不再把入口称为十个步骤或交互工作流。

- [x] **S10-02：改为功能舱菜单语义**
  验收：前台使用 VR 主页、任务舱、讲解舱、书写台、笔画台、创作台、档案舱、复盘舱、报告舱和巩固舱。

- [x] **S10-03：真实状态仍来自本机记录**
  验收：菜单状态继续由本机任务、讲解、练习、作品、报告和计划推导，不改成静态展示。

- [x] **S10-04：动作详情同步功能舱语义**
  验收：跳转和阶段记录显示“目标功能舱”“VR 菜单”，不再显示“目标流程”。

完成记录（2026-06-15）：

- 已把前台可见入口改成真实 VR 菜单，并保留 `?flow=` 与旧 `?step=` 兼容路由。
- 已把任务要点从编号列表改为非线性要点列表，避免与旧演示步骤混淆。
- 已更新 E2E 断言，继续覆盖真实讲解、练习、作品、报告、复盘和巩固动作。

## S11 写实桌面整合进基础场景

- [x] **S11-01：删除前台写实样张入口**
  验收：首页不再显示“写实 3D 样张”链接，smoke 不再要求前台包含 `realistic-demo.html` 入口。

- [x] **S11-02：写实样张内容放入基础桌面**
  验收：基础 Three.js 渲染器包含隐藏 `front-writing-desk-layer`，其中有宣纸、墨迹、毛笔、砚台、印章和透明讲解屏。

- [x] **S11-03：默认隐藏，书写时显示**
  验收：首页默认 `MR_WRITING_DESK_VISIBLE=false`；进入 `practice` / “临摹”书写台后为 `true`，`body` 带 `is-writing-desk-mode`。

- [x] **S11-04：桌面和移动端 3D 验收**
  验收：桌面 1440x920 与手机 390x844 下，基础场景和书写场景 WebGL 像素采样均非空白，截图保存在 `test-results/writing-desk-check/`。

完成记录（2026-06-17）：

- 已把写实样张桌面核心内容整合到基础场景桌面，作为书写台专用隐藏层。
- 已保留 `realistic-demo.html` / `realistic-admin.html` 作为后台验证和历史回归页面，但前台首页不再显示独立入口。
- 已更新 `tests/e2e/real-flows.spec.js`，开始临摹后断言书写桌面层真实显示。

## S12 前台两级交互菜单

- [x] **S12-01：10 个外显入口收敛为 3 个一级方向**
  验收：前台可见一级菜单只有先看看、开始写、看结果。

- [x] **S12-02：一级确认后显示二级动作**
  验收：选择一级方向后，同一主面板显示最多 3 个二级动作，二级动作连接真实路由或真实本机处理。

- [x] **S12-03：只保留一个可见交互框**
  验收：左侧路径、底部 Dock、快速切换、空间模式独立框、场景编辑框和右侧三张反馈卡均不再可见。

- [x] **S12-04：移除场景识别、实时反馈、历史记录浮动卡**
  验收：前台 DOM 不再包含 `.insight-stack`，页面不再显示这三个独立浮动块。

完成记录（2026-06-17）：

- 新增 `VR_MENU_GROUPS` 两级菜单模型，保留内部 10 个 `SCENES` 用作路由和真实功能承载。
- 已更新 `clickWorkflow()` E2E helper，测试通过两级菜单进入讲解、档案、报告和巩固。
- 浏览器验证 `visiblePanels` 仅包含 `.mr-main-panel`，一级菜单 3 个、二级动作 3 个。

## S13 恢复书写台宣纸永字

- [x] **S13-01：恢复旧版永字墨迹贴图**
  验收：`front-main-scene-renderer.js` 的 `createCalligraphyTexture()` 使用轻量墨迹叠加保留旧版质感，宣纸纹理自身绘制矢量笔触“永”字作为稳定主视觉。

- [x] **S13-02：避免墨迹与宣纸深度冲突**
  验收：`writing-ink-character` 使用双面透明材质、polygon offset，并略微抬高到宣纸上方。

- [x] **S13-03：保持书写场景触发逻辑**
  验收：首页默认隐藏写实书写层，进入“开始写 / 练字”后显示宣纸和“永”字。

- [x] **S13-04：进入书写台时隐藏旧占位物**
  验收：书写台模式下旧版 `desktop-paper`、`desktop-inkstone`、`desktop-gold-brush` 和 `desktop-red-brush` 不再盖住写实书写层。

- [x] **S13-05：书写台使用桌面俯视近景**
  验收：进入“练字”时默认 pitch 为桌面上方视角，白色宣纸不被桌面横梁遮挡。

完成记录（2026-06-17）：

- 已把基础书写场景白色宣纸上的“永”字恢复为旧版样张的可见墨迹效果。
- 已保留当前两级交互菜单和写实桌面默认隐藏逻辑。
- 已用浏览器截图验证进入“开始写 / 练字”后 `MR_WRITING_DESK_VISIBLE=true`，白色宣纸和“永”字可见。

## S14 前台适老化减字

- [x] **S14-01：一级菜单改为老人可理解短词**
  验收：前台一级菜单显示“先看看 / 开始写 / 看结果”。

- [x] **S14-02：二级动作压缩为短按钮**
  验收：二级动作显示“选字 / 听讲解 / 去练字”等短词，真实动作映射不变。

- [x] **S14-03：主状态和任务卡减字**
  验收：学习状态摘要只显示今日字、练习次数和作品数量；任务卡只显示字、状态、进度和下一步。

- [x] **S14-04：标题和模式切换减负**
  验收：顶部标题显示“今日：永”等短标题，主面板默认隐藏学习模式切换。

- [x] **S14-05：复杂信息默认收起**
  验收：热点说明、任务库、任务步骤和审计列表不再默认占用主视野，DOM 和导出能力保留。

完成记录（2026-06-17）：

- 已把前台主交互改成适老化短文案和大按钮。
- 已保留原有讲解、临摹、拆解、创作、档案、报告和计划真实功能。

## S15 主面板下方小字继续收起

- [x] **S15-01：隐藏默认学习状态摘要**
  验收：老人默认视野不再显示“今日字 / 练习次数 / 作品数量”这类系统摘要。

- [x] **S15-02：隐藏二级选择提示**
  验收：一级和二级按钮之间不再显示额外小字提示，只保留按钮本身。

- [x] **S15-03：隐藏服务边界和任务说明卡**
  验收：服务边界状态、今日任务说明、任务进度和任务库不再占用前台主视野。

- [x] **S15-04：保留数据与测试证据**
  验收：对应 DOM、服务边界文本和任务状态仍存在，Playwright 继续验证真实数据没有被删除。

- [x] **S15-05：压短默认动作反馈**
  验收：默认反馈和操作后反馈只显示“已完成 / 下一步 / 查看 / 开始练字 / 作品已保存”类短句，不再把任务说明和推荐入口写成段落。

- [x] **S15-06：收起书写和评分下方说明**
  验收：书写格状态、评分卡和评分服务说明默认不可见，只保留书写区域和真实操作按钮。

- [x] **S15-07：首页隐藏旧动作区**
  验收：首页不再露出旧动作按钮、动作反馈和审计文字；进入具体功能舱后真实操作区再出现。

- [x] **S15-08：动作详情和审计默认不展开**
  验收：动作详情、学习审计、链接审计不再作为老人默认可见内容；DOM、导出按钮和自动化验证继续保留。

完成记录（2026-06-17）：

- 已把主面板下方说明类小字继续从默认前台收起。
- 已把默认和操作后动作反馈压缩为短提示，详细说明仍保留在真实动作详情和审计数据中。
- 已收起书写格状态、评分卡和评分服务说明，减少主面板下半区的小字密度。
- 已为前台设置 `data-learning-scene`，首页隐藏旧动作区，具体功能舱继续保留真实按钮。
- 已把动作详情和审计面板改为默认不展开，测试改为读取隐藏 DOM 并程序化触发导出验证。
- 已更新移动端和真实书写流程测试，避免把开发审计信息重新要求成可见 UI。

## S16 详细区隐藏与大按钮化

- [x] **S16-01：二级菜单横排大按钮**
  验收：首页、书写页和结果页的二级菜单不再纵向堆三行，减少向下滚动。

- [x] **S16-02：旧动作按钮短词化**
  验收：动作区显示“练字 / 对比 / 看笔画 / 保存 / 导报告 / 计划”等短词，不再显示长动作名。

- [x] **S16-03：隐藏老人主视图里的详细面板**
  验收：讲解、复盘、报告、档案、作品集、计划和回执审计不再默认显示在主交互框下方。

- [x] **S16-04：保留真实功能验证**
  验收：E2E 使用 `e2e-show-details` 临时展开隐藏证据区，继续验证导出、审计、报告和仓库能力。

完成记录（2026-06-17）：

- 已把“再下面一堆小字”的主要来源从前台老人视图移除。
- 已确认首页无滚动，结果页无滚动，书写页只保留大按钮和书写格。

## S17 主面板零小字审计

- [x] **S17-01：隐藏系统眉标**
  验收：主面板不再显示 `AI 书法教练`、`VR 交互菜单`、`已选：...` 等小字号标签。

- [x] **S17-02：标题放大**
  验收：主面板顶部标题字号为 22px，不再被 18px 规则覆盖。

- [x] **S17-03：可见小字数量为 0**
  验收：首页、书写页、结果页主面板内可见字号小于 16px 的文本数量均为 0。

- [x] **S17-04：无内部滚动**
  验收：首页、书写页、结果页主面板 `scrollHeight - clientHeight` 均为 0。

完成记录（2026-06-17）：

- 已把主面板从“有系统说明的小控制台”进一步改成“标题 + 大按钮”的适老化交互框。
- 已用 Playwright 截图和字号审计验证，不再出现主面板下方小字堆叠。

## S18 全页面残余小字补漏

- [x] **S18-01：隐藏左上场景状态胶囊**
  验收：前台不再显示“已完成 / 进行中 / 待完成”这类与老人当前操作无关的小字。

- [x] **S18-02：主按钮统一不低于 18px**
  验收：一级按钮、二级按钮和具体动作按钮的可见文字均不低于 18px。

- [x] **S18-03：放大书写工具按钮**
  验收：书写台底部工具按钮为 40px 方形按钮，按钮文字为 18px，移动端不贴边、不遮挡宣纸。

- [x] **S18-04：全页面小字审计清零**
  验收：首页、书写页、结果页中自有可见文本字号小于 18px 的项目均为 0，主面板溢出为 0。

完成记录（2026-06-17）：

- 已完成老人前台全页面残余小字补漏，保留真实书写、报告、档案和计划能力。
- 已保留测试截图到 `test-results/senior-menu-check/`，用于后续继续追踪小字号回归。

## S19 老人前台分阶段菜单

- [x] **S19-01：首页只显示一级选择**
  验收：首页主面板只显示“先看看 / 开始写 / 看结果”3 个大按钮，不再同时显示二级动作。

- [x] **S19-02：确认一级后只显示二级动作**
  验收：选择一级方向后，主面板只显示该组 3 个二级动作，并通过图标按钮返回上一步。

- [x] **S19-03：功能舱隐藏菜单堆叠**
  验收：进入练字、报告、档案和计划后，一级/二级菜单均不可见，只保留当前功能的真实操作按钮。

- [x] **S19-04：直达链接同样简化**
  验收：直接打开 `?flow=practice`、`?flow=report`、`?flow=history`、`?flow=plan` 时没有菜单堆叠、没有主面板溢出、没有 18px 以下可见文字。

完成记录（2026-06-17）：

- 已把前台菜单改成 `primary / secondary / feature` 三种互斥阶段，减少老人一次看到的按钮数量。
- 已更新 Playwright `clickWorkflow()`，测试从首页三按钮入口进入真实功能，不依赖隐藏菜单。
- 已保存阶段截图到 `test-results/senior-menu-check/stage-*.png`。

## S20 主面板标题动作词化

- [x] **S20-01：首页标题改成直接指令**
  验收：首页主面板标题显示“请选择”，不再显示 `AI 书法教练` 这类系统身份。

- [x] **S20-02：二级选择标题跟随一级方向**
  验收：选择一级方向后，标题显示“先看看 / 开始写 / 看结果”，不再需要额外说明小字。

- [x] **S20-03：功能舱标题统一动作词**
  验收：练字、报告、档案、计划等页面显示“开始练字 / 看报告 / 看记录 / 练习计划”等动作词，不显示“实时引导 / 学习数据”等抽象词。

- [x] **S20-04：标题短词化审计通过**
  验收：首页、二级选择、练字页、报告页主面板溢出为 0，18px 以下可见文字为 0。

完成记录（2026-06-17）：

- 已新增 `getSeniorPanelTitle()`，让主面板标题跟随 `primary / secondary / feature` 三种阶段切换。
- 已保存标题短词化截图到 `test-results/senior-menu-check/title-*.png`。

## S21 老人前台触控目标放大

- [x] **S21-01：返回和收起按钮不小于 44px**
  验收：首页、二级页和功能页可见返回/收起按钮均为 46px 触控目标。

- [x] **S21-02：书写工具按钮不小于 44px**
  验收：撤销、清空、回放按钮均为 46px 触控目标，移动端不贴边。

- [x] **S21-03：移除非必要点阵干扰**
  验收：老人主面板不再显示点阵拖拽柄，标题栏仍可拖动面板。

- [x] **S21-04：触控审计通过**
  验收：首页、二级页、练字页所有可见按钮均不小于 44px，主面板溢出为 0，18px 以下可见文字为 0。

完成记录（2026-06-17）：

- 已把顶部图标按钮和书写工具按钮统一放大到 46px。
- 已保存触控尺寸截图到 `test-results/senior-menu-check/touch-*.png`。

## S22 功能页按钮口语化

- [x] **S22-01：抽象按钮改为口语动作**
  验收：前台功能页显示“开始听 / 看范字 / 比一比 / 保存作品 / 看好字 / 分享作品 / 再练笔”等口语动作词。

- [x] **S22-02：功能页最多 3 个动作按钮**
  验收：`task/lecture/practice/stroke/creation/history/review/report/plan` 每页可见动作按钮数量均不超过 3 个。

- [x] **S22-03：真实动作映射不变**
  验收：按钮短词只改变显示文案，仍调用原有讲解、临摹、保存作品、导报告、制定计划等真实本机动作。

- [x] **S22-04：按钮口语化审计通过**
  验收：10 个 `?flow=` 页面主面板溢出为 0，18px 以下可见文字为 0。

完成记录（2026-06-17）：

- 已为 `getSeniorActionLabel()` 增加适老化口语按钮映射。
- 已保存动作按钮审计截图到 `test-results/senior-menu-check/senior-actions-*.png`。

## S23 菜单按钮纯短词化

- [x] **S23-01：移除按钮隐藏说明文本**
  验收：首页和二级页菜单按钮的 `textContent` 等于可见短词，不再包含隐藏解释说明。

- [x] **S23-02：首页不默认高亮**
  验收：首页 3 个一级入口均不带 `is-active`，`aria-pressed=false`，用户选择前不暗示默认选项。

- [x] **S23-03：说明转为非视觉辅助**
  验收：按钮说明只保留在 `title` 等辅助信息中，不占用老人默认视觉和 DOM 文本。

- [x] **S23-04：纯短词审计通过**
  验收：首页和二级页隐藏说明文本数量为 0，主面板溢出为 0，18px 以下可见文字为 0。

完成记录（2026-06-17）：

- 已移除一级/二级菜单按钮里的隐藏说明 `span`。
- 已保存纯短词菜单截图到 `test-results/senior-menu-check/pure-menu-*.png`。

## S24 底部提示条适老化短词

- [x] **S24-01：全局操作提示短词化**
  验收：老人正常访问时，底部 `noticeState` 显示“已保存 / 已打开 / 已导出 / 暂不能用 / 看全景”等短词，不再显示完整技术句。

- [x] **S24-02：完整信息保留给诊断**
  验收：完整提示写入 `title` 和 `data-full-message`；E2E 诊断模式仍能读取完整提示，方便长期自动化验证。

- [x] **S24-03：提示条字号适老化**
  验收：底部提示条桌面和手机视口均不低于 20px，中央错误提示不低于 19px。

- [x] **S24-04：模型展示提示压缩**
  验收：进入模型/VR 环视类提示时，老人只看到“看全景”，不再看到一整句物件列表。

- [x] **S24-05：提示条不挡操作**
  验收：底部提示条设置为非点击目标，出现时不拦截后方按钮、表单和导出入口。

完成记录（2026-06-17）：

- 已新增前台系统提示短词映射，覆盖保存、复制、导出、发布、导入、打开、切换、失败、冲突和模型展示。
- 已把底部提示条改为大字号短词反馈，完整文本保留在辅助属性中，且不会拦截操作点击。

## S25 操作后反馈短词大字化

- [x] **S25-01：动作反馈压缩为短词**
  验收：老人正常访问时，`actionFeedback` 显示“已选字 / 开始听 / 开始练 / 已保存 / 已生成 / 已计划”等短词，不再显示长句或带冒号的说明。

- [x] **S25-02：功能页默认反馈短词化**
  验收：刚进入讲解、练字、报告或计划功能页时，默认反馈显示短动作词，不再显示“下一步：...”说明句。

- [x] **S25-03：诊断模式保留完整状态**
  验收：E2E 诊断模式仍能读取“开始练字 / 报告已生成 / 计划已生成”等完整反馈，保证真实流程验收不丢细节。

- [x] **S25-04：反馈条适老化字号**
  验收：按钮下方反馈条不低于 20px，居中显示，最多承担短词状态反馈。

- [x] **S25-05：真实动作不变**
  验收：短词只改变显示文案，不改变讲解播放、书写训练、保存作品、报告导出和学习计划生成逻辑。

完成记录（2026-06-17）：

- 已把 `getSeniorFeedbackMessage()` 和 `getLearningActionHint()` 区分普通老人模式和诊断模式。
- 已把 `.mr-main-panel .action-feedback` 改为 20px 粗体短反馈样式。

## S26 老人模式隐藏详情总闸门

- [x] **S26-01：默认界面只显示核心交互**
  验收：普通老人模式下，主面板默认只显示标题、两级菜单、功能按钮、书写画布和短反馈。

- [x] **S26-02：详情与审计不回流**
  验收：学习摘要、服务边界、任务库、热点内容、动作详情、审计、评分卡、讲解详情、复盘、报告、档案和计划详情都被普通模式总闸门隐藏。

- [x] **S26-03：诊断模式仍可展开**
  验收：`body.e2e-show-details` 下仍能显示讲解、复盘、报告、档案、计划、动作详情和审计，用于自动化验收。

- [x] **S26-04：滚动审计清零**
  验收：`home/lecture/practice/stroke/creation/history/review/report/plan` 在手机和桌面视口滚动到主面板顶部、中部、底部，均无可见 18px 以下文字或 18 字以上长句。

完成记录（2026-06-17）：

- 已为普通老人模式增加 `.mr-main-panel` 详情总闸门。
- 已保留 E2E 诊断模式展开能力。

---

## 当前验收命令

```bash
npm run build
PLAYWRIGHT_BASE_URL=http://localhost:41496/ npx playwright test tests/e2e/real-flows.spec.js
git diff --check
npm audit --audit-level=high
```

完成记录（2026-06-14）：

- `npm run build` 通过：26 个脚本、4 个页面冒烟检查通过。
- `PLAYWRIGHT_BASE_URL=http://localhost:41496/ npx playwright test tests/e2e/real-flows.spec.js` 通过：24 passed。
- `git diff --check` 通过。
- `npm audit --audit-level=high` 通过，0 vulnerabilities。
- 本轮将以中文提交并推送到 GitHub `main`。
